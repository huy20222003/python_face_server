[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[env]
NIXPACKS_APT_INSTALL = "libgl1-mesa-glx libglib2.0-0 libsm6 libxext6 libxrender-dev"

[start]
cmd = "python server.py"
